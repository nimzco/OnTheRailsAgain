%aside.date
 
%section.articles  
  - @articles.each do |article|
    = render article
  = will_paginate @articles, :previous_label => "Précédent", :next_label => "Suivant"

%aside.tags
  .content
  
    = render "shared/search"
    
    #tags.aside_box
      %h3 Tags
      %ul
        - @tags.each do |tag|
          %li= link_to "#{tag.name} (#{tag.articles.length})", articles_path(:tag => tag.name)
    #contact.aside_box
      %h3 
        %a{:href => "mailto:admin@ontherailsagain.com"} Nous contacter
      %p
        N'hésitez pas à nous envoyer un mailpour nous demander un article ou ne serais-ce que pour prendre contact avec nous !
        
    
= content_for :javascript do
  :coffeescript
    $( () ->
      $('#advance_search').toggle()
      $('#advance_search_button').click( ->
        $('#advance_search').slideToggle()
      )
    )