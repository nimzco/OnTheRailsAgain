= form_for @search, {:controller => 'articles', :action => 'index', :html => { :class => "navbar-search span4"} } do |f|
  = f.text_field  :title_or_content_contains, :size => '15', :class => 'search-query', :placeholder => 'Titres, tags, auteurs, etc…'
  - f.submit  "&nbsp;".html_safe, :id => 'search_submit'
