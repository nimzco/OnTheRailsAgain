$('#comment_form').remove();
<% if params[:new_comment] %>
  $('#comments').append("<%= escape_javascript(render @comment) %>");
<% else %>
  $('#comment_<%= @comment.parent_id %>').after("<%= escape_javascript(render @comment) %>");
<% end %>
if ($('#new_answer_link').length === 0) {
  $('#comments').after('<%= link_to "Nouvelle réponse", new_article_comment_path(@article, :new_comment => true), :remote => true, :id => "new_answer_link" %>');
}
var text = $('#comments_header').text()
var n = parseInt(text[0]) + 1
var newText = text.slice(1)
$('#comments_header').text(n + newText)
